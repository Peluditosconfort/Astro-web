---
interface Props {
    textoBoton: string;
}

const { textoBoton } = Astro.props;
---

<div>
    <a href="#"
        class="rounded-lg sm:w-auto h-14 cursor-pointer flex items-center justify-center border border-naranja bg-transparent hover:bg-naranja active:bg-white active:border-naranja text-lg text-naranja hover:text-white font-black text-base transition-all duration-300"
        >{textoBoton}</a
    >
</div>
