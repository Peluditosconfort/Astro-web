---
const { estilos } = Astro.props;
const { text } = Astro.props;

const { url} = Astro.props;
---

<a
    href={ url }
    class={`font-lato font-bold sm:w-full lg:w-auto rounded text-center transition-all duration-500 ease-in-out transform active:bg-naranja ${estilos}`}
    >{text}</a
>
